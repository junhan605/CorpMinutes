<%= form_for(company) do |f| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="field">
    <%= f.label :president %>
    <%= f.text_field :president %>
  </div>

  <div class="field">
    <%= f.label :treasure %>
    <%= f.text_field :treasure %>
  </div>

  <div class="field">
    <%= f.label :secretary %>
    <%= f.text_field :secretary %>
  </div>


  <%= f.hidden_field :user_ids, :value => Array[current_user[:id]] %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
